# Quick Task 260702-gaf: Editable Article Folders

## Request

Create folders so the first article can be added as a text file.

## Plan

- Add an `Articles/` folder with a first article folder.
- Add a simple `article.txt` template format for beginner editing.
- Add `Config/articles.txt` to list article folders in display order.
- Add a small Articles tab renderer that reads configured folders and text files.
- Add article list/detail styling consistent with the current portfolio.
- Validate JavaScript syntax and diff hygiene.