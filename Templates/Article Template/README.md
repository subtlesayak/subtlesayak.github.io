# Article Template

Copy this folder into `Articles/`, rename it, edit `article.txt`, and add the folder name to `Config/articles.txt`.

Edit `author-note.txt` to show a brief note after the article, or delete it when no note is needed.
