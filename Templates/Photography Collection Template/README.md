# Photography Collection Template

Copy this folder into `Projects/Photography/Collections/`, rename it, add full-size photos beside `entry.txt`, and add matching thumbnail files to `thumbs/` when available.

Then add the folder name to `Projects/Photography/collections.txt`.
